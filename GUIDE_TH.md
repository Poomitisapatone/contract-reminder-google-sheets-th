# คู่มือติดตั้งระบบแจ้งเตือนสัญญาจัดซื้อจัดจ้าง

ชุดนี้เป็นแบบ “ไฟล์แม่แบบ + Apps Script” สำหรับนำไปติดตั้งใน Google Sheets ของผู้ใช้แต่ละคน โดย Google Sheets ต้นฉบับของผู้พัฒนาไม่ถูกเปิดเผยและไม่ได้รวมอยู่ในชุดดาวน์โหลด

## ไฟล์ในชุด

- `Contract_Reminder_GoogleSheets_Template.xlsx` — แม่แบบที่ล้างข้อมูลแล้ว
- `Code.gs` — ระบบแจ้งเตือน อีเมล Google Calendar และเมนูใช้งาน
- `appsscript.json` — Manifest และสิทธิ์ที่ระบบต้องใช้
- `GUIDE_TH.md` — คู่มือฉบับนี้
- `.gitignore` — ป้องกันไฟล์ลับที่พบบ่อยเมื่ออัปขึ้น GitHub

> ไฟล์ Excel ไม่สามารถเก็บ Google Apps Script ไว้ภายในได้ จึงต้องติดตั้ง `Code.gs` แยกตามขั้นตอนด้านล่าง

## ความสามารถหลัก

- เก็บชื่อบริษัท เบอร์ติดต่อ ชื่อรายการ และอีเมลแจ้งเตือน
- รองรับผู้รับ Gmail, Outlook และอีเมลองค์กร
- คำนวณวันครบกำหนดจากวันที่เซ็นสัญญาและจำนวนวันส่งมอบ
- แสดงจำนวนวันที่เหลือและสถานะอัตโนมัติพร้อมสี
- แสดง `ส่งก่อนกำหนด 1 วัน`, `ส่งตรงกำหนด` หรือ `ส่งล่าช้า ... วัน` เป็นจำนวนวันเต็ม
- เลือกการส่งมอบจาก Dropdown `ยังไม่ส่ง` หรือ `ส่งแล้ว`
- บันทึกวันที่ส่งจริงอัตโนมัติเมื่อเปลี่ยนเป็น `ส่งแล้ว`
- ส่งอีเมลยืนยันเมื่อเปลี่ยนเป็น `ส่งแล้ว`
- แจ้งเตือนก่อนครบกำหนด 10, 5, 3 วัน และวันครบกำหนด
- สร้างและปรับปรุงกิจกรรมใน Google Calendar
- มี Dashboard, Timeline, หน้าตั้งค่า และประวัติระบบ
- เพิ่มแถวพร้อมใช้ครั้งละ 50 หรือ 100 แถวจากเมนู

## สิ่งที่ต้องมี

- บัญชี Google
- Google Drive และ Google Sheets
- สิทธิ์ส่งอีเมลและใช้งาน Google Calendar ของบัญชีผู้ติดตั้ง

ไม่ต้องมีเซิร์ฟเวอร์ โดเมน หรือฐานข้อมูล ค่าใช้จ่ายทั่วไปเป็นศูนย์ภายในโควตาของ Google Apps Script

## วิธีติดตั้งจากไฟล์แม่แบบ

1. ดาวน์โหลด ZIP และแตกไฟล์
2. อัปโหลด `Contract_Reminder_GoogleSheets_Template.xlsx` ไปยัง Google Drive
3. เปิดไฟล์ด้วย Google Sheets
4. เลือก `ไฟล์ > บันทึกเป็น Google ชีต` เพื่อแปลงเป็นไฟล์ Google Sheets จริง
5. เปิด `ส่วนขยาย > Apps Script`
6. เปิดไฟล์ `Code.gs` ในชุดดาวน์โหลด คัดลอกทั้งหมด แล้วนำไปแทนที่โค้ดเดิมใน Apps Script
7. เปิด `การตั้งค่าโปรเจกต์` และเปิดตัวเลือกแสดงไฟล์ Manifest `appsscript.json`
8. เปิด `appsscript.json` ใน Apps Script แล้วแทนที่ด้วยไฟล์จากชุดดาวน์โหลด
9. กดบันทึก
10. เลือกฟังก์ชัน `setupSystem` แล้วกด `Run`
11. อนุญาตสิทธิ์ Spreadsheet, Calendar, การส่งอีเมล การสร้าง Trigger และอีเมลผู้ใช้
12. กลับไป Google Sheets แล้วรีเฟรชหน้า

เมื่อติดตั้งสำเร็จ จะเห็นเมนู `ระบบสัญญา V2`

## ตั้งค่าก่อนใช้งาน

เปิดแท็บ `ตั้งค่า` แล้วตรวจสอบค่าต่อไปนี้

| รายการ | ค่าเริ่มต้น | ความหมาย |
|---|---:|---|
| เขตเวลา | `Asia/Bangkok` | เขตเวลาที่ใช้คำนวณและแสดงผล |
| Calendar ID | `primary` | ปฏิทินหลักของบัญชีผู้ติดตั้ง |
| ชื่อผู้ส่ง | `ระบบแจ้งเตือนสัญญา` | ชื่อที่แสดงในอีเมล |
| ชั่วโมงตรวจแจ้งเตือน | `8` | ให้ Trigger ทำงานหนึ่งครั้งในช่วงชั่วโมง 08:00–08:59 |
| วันแจ้งเตือน | `10,5,3,0` | ระดับที่โค้ดรุ่นนี้รองรับ |
| จำนวนแถวสูงสุด | `500` | จำนวนแถวข้อมูลที่ระบบตรวจ |
| อีเมลทดสอบ | ว่าง | กรอกอีเมลของผู้ติดตั้งก่อนทดสอบ |
| วิธีนับวัน | นับวันถัดจากวันเซ็น | เลือกได้ว่าจะนับรวมวันเซ็นหรือไม่ |
| ซิงก์ Calendar ทันที | เปิด | ปรับ Calendar เมื่อแก้ข้อมูล |
| ล้างวันที่ส่งเมื่อกลับเป็นยังไม่ส่ง | เปิด | ล้างวันที่ส่งจริงเมื่อย้อนสถานะ |
| แจ้งเตือนเมื่อเปลี่ยนเป็นส่งแล้ว | เปิด | ส่งอีเมลยืนยันการส่งมอบ |

หากแก้ `ชั่วโมงตรวจแจ้งเตือน` ให้รัน `setupSystem` อีกครั้ง เพื่อสร้าง Trigger ตามชั่วโมงใหม่

> Trigger ของ Google Apps Script ไม่รับประกันนาทีเป๊ะ หากตั้งเป็น `8` ระบบจะทำงานหนึ่งครั้งในช่วง 08:00–08:59 ไม่จำเป็นต้องตรง 08:00

## คอลัมน์ที่ผู้ใช้กรอก

กรอกเฉพาะคอลัมน์ต่อไปนี้ในแท็บ `รายการสัญญา`

- ชื่อบริษัท
- เบอร์ติดต่อ
- ชื่อรายการ
- อีเมลแจ้งเตือน (คั่นหลายอีเมลด้วยเครื่องหมายจุลภาค)
- วันที่เซ็นสัญญา
- กำหนดส่ง (วัน)
- วันที่ส่งจริง — ปล่อยว่างได้ ระบบจะกรอกเมื่อเลือก `ส่งแล้ว`
- การส่งมอบ — เลือก `ยังไม่ส่ง` หรือ `ส่งแล้ว`
- หมายเหตุ

คอลัมน์วันที่ครบกำหนด วันที่เหลือ สถานะ และลิงก์ Calendar เป็นสูตรอัตโนมัติ ส่วนคอลัมน์ N–X เป็นข้อมูลระบบ ไม่ควรแก้ไขเอง

## ทดสอบอีเมล

1. กรอก `อีเมลทดสอบ` ในแท็บ `ตั้งค่า`
2. เลือก `ระบบสัญญา V2 > ทดสอบส่งอีเมล`
3. ตรวจ Inbox และ Spam/Junk
4. หากต้องการทดสอบรายการจริง ให้กรอกข้อมูลหนึ่งรายการแล้วเลือก `ตรวจและส่งแจ้งเตือนตอนนี้`

อีเมลถูกส่งจากบัญชี Google ของผู้ติดตั้ง ผู้รับ Outlook รับข้อความได้ตามปกติ แต่ระบบไม่ได้ส่งจากบัญชี Outlook

## Google Calendar

- ใช้ `primary` เพื่อสร้างกิจกรรมในปฏิทินหลักของบัญชีผู้ติดตั้ง
- เมื่อแก้บริษัท รายการ วันเซ็น หรือวันครบกำหนด ระบบจะซิงก์กิจกรรมเมื่อเปิดการซิงก์ทันที
- ใช้เมนู `ซิงก์ Google Calendar ทั้งหมด` เพื่อสั่งซิงก์ใหม่ทุกแถว
- หากเลือก `ส่งแล้ว` ระบบจะอัปเดตชื่อกิจกรรมและปิดการแจ้งเตือนของรายการนั้น

## การเพิ่มแถวและ Dashboard

- แม่แบบเริ่มต้นมี 500 แถวพร้อมใช้
- ใช้เมนู `ระบบสัญญา V2 > เพิ่มแถวพร้อมใช้` เพื่อเพิ่ม 50 หรือ 100 แถว
- Dashboard อ้างอิงข้อมูลถึงแถว 2,000 จึงอัปเดตอัตโนมัติเมื่อเพิ่มข้อมูลภายในช่วงนี้

## การแชร์อย่างปลอดภัย

รุ่นนี้ออกแบบให้ผู้ใช้แต่ละคนใช้ไฟล์ของตนเอง

- หากให้ผู้อื่นดูอย่างเดียว ให้แชร์เป็น `Viewer` หรือ `Commenter`
- หากต้องการแจกให้ผู้อื่นใช้งาน ให้ส่ง ZIP หรือแชร์แม่แบบแบบ `Viewer` แล้วให้ผู้รับสร้างสำเนา
- อย่าให้สิทธิ์ `Editor` ในไฟล์ใช้งานจริงแก่คนที่ไม่ควรแก้ข้อมูล เพราะ Editor สามารถเปลี่ยนสถานะและข้อมูลในตารางได้
- ผู้รับ ZIP ไม่มีสิทธิ์เข้าถึง Google Sheets, Gmail หรือ Google Calendar ต้นฉบับของผู้พัฒนา

## อัปโหลดขึ้น GitHub แบบ Public

อัปโหลดได้เฉพาะไฟล์ต่อไปนี้

- `Code.gs`
- `appsscript.json`
- `GUIDE_TH.md`
- `.gitignore`
- `Contract_Reminder_GoogleSheets_Template.xlsx`

ห้ามอัปโหลดไฟล์ต่อไปนี้

- `.clasp.json` และ `.clasprc.json`
- `client_secret*.json`, `credentials*.json` และไฟล์ Service Account
- `.env`, Private Key, API key, token หรือรหัสผ่าน
- Google Sheets หรือไฟล์ Excel ที่มีข้อมูลสัญญาจริง

ตรวจ `git status` ก่อน Commit ทุกครั้ง แม้จะมี `.gitignore` แล้วก็ตาม

## แก้ปัญหาเบื้องต้น

### ไม่เห็นเมนูระบบสัญญา V2

รีเฟรช Google Sheets หรือรันฟังก์ชัน `onOpen` จาก Apps Script หนึ่งครั้ง

### ส่งอีเมลไม่ได้

- ตรวจรูปแบบอีเมลผู้รับ
- ตรวจสิทธิ์ของ Apps Script
- ตรวจโควตาการส่งอีเมลของบัญชี Google
- เปิดแท็บ `ประวัติระบบ` เพื่อตรวจข้อความผิดพลาด

### Calendar ไม่ถูกสร้าง

- ตรวจว่า Calendar ID เป็น `primary`
- อนุญาตสิทธิ์ Calendar ให้ครบ
- ตรวจว่ากรอกชื่อบริษัท ชื่อรายการ และวันที่ครบกำหนดแล้ว
- ใช้เมนู `ซิงก์ Google Calendar ทั้งหมด`

### เปลี่ยนเวลาแล้ว Trigger ยังทำงานเวลาเดิม

รัน `setupSystem` อีกครั้ง เพื่อให้ระบบลบ Trigger เดิมและสร้างใหม่

## ความเป็นส่วนตัว

แม่แบบในชุดนี้ไม่มีอีเมล เบอร์โทร ชื่อบริษัท รายการสัญญา Spreadsheet ID, Calendar Event ID, API key, token, รหัสผ่าน หรือประวัติผู้ใช้ ข้อมูลที่กรอกหลังติดตั้งจะอยู่ในบัญชี Google ของผู้ติดตั้งแต่ละคน
